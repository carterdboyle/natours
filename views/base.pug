doctype html
html
    head
        title Natours | #{tour}
        //- #{} interpolation syntax - like a template string
        link(rel='stylesheet' href='css/style.css')
        link(rel='shortcut icon' type='image/png' href='img/favicon.png')
    body
        h1= tour
        h2= user.toUpperCase()
        //- h1 The Park Camper

        - const x = 9;
        h2= 2 * x

        p This is just some text